[package]
name = "jrsx"
version = "0.1.0"
edition = "2021"

[lib]
doctest = false

[features]
default = ["config"]
config = ["derive/config"]

[dependencies]
derive = { path = "../derive" }

[dev-dependencies]
askama = "0.12.1"
